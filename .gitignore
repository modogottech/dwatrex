# Build output
build/
dist/
installer_output/
*.spec
version_info.py

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local database (per-machine data — never commit)
*.db
*.db-wal
*.db-shm

# Note: bundled web assets (frontend/assets/fonts/*.woff2 and chart.umd.min.js)
# are committed to the repo so the app works fully offline and from source with
# no download step. Run frontend/assets/fetch_assets.py once to populate them.

# OS noise
.DS_Store
Thumbs.db

DwatrexSetup*
